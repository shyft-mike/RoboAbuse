using UnityEngine;

/// <summary>
/// A marker class to represent a point that can be attached to.
/// </summary>
public class AttachPoint : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
